<!doctype html>
<html class="no-js" lang="">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible" content="ie=edge">
        <title>{{ title }}</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="../cc-layout-builder.css">
    </head>
    <body>
        <main>
            <cc-layout-builder class="cc-layout-builder" assets-src="{{ asset( '' ) }}" :add-component="generateComponent" :edit-component="generateComponent">
            </cc-layout-builder>
        </main>

        <script src="//cdnjs.cloudflare.com/ajax/libs/vue/1.0.26/vue.js"></script>
        <script src="../cc-layout-builder.js"></script>
        <script>
            new Vue( {
                el: 'body',
                components: {
                    'cc-layout-builder': ccLayoutBuilder.default
                },
                methods: {
                    /**
                     * Generate some random component data.
                     */
                    generateComponent( currentComponent ) {
                        console.log( currentComponent );
                        return {
                            name: Date.now(),
                            id: Math.random().toString( 36 ).replace( /[^a-z]+/g, '' ).substr( 0, 10 ),
                            settings: {}
                        }
                    }
                }
            } );
        </script>
    </body>
</html>
