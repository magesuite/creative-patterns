<button class="action-button {{ classes }} | {{ mix }}" {{ attributes }}>
    {% if icon.svgId %}
        <svg class="action-button__icon {{ icon.classes }}"><use xlink:href="/images/sprites.svg#{{ icon.svgId }}"></use></svg>
    {% endif %}
    {{ text }}
</button>
