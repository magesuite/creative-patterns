<button class="action-button {{ classes }} | {{ mix }}" {{ state }}>
    {% if icon %}
        <svg class="action-button__icon action-button__icon--{{ icon }}"><use xlink:href="/images/sprites.svg#icon_{{ icon }}"></use></svg>
    {% endif %}
    {{ text }}
</button>
