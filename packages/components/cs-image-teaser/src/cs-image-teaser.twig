<figure class="cs-image-teaser">
    <img class="cs-image-teaser__image" src="{{ imageSrc }}" alt="{{ imageAlt }}">
    <figcaption class="cs-image-teaser__caption">
        <span class="cs-image-teaser__text-secondary">{{ textSecondary }}</span>
        <span class="cs-image-teaser__text-primary">{{ textPrimary }}</span>
    </figcaption>
</figure>