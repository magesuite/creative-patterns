<div class="{{ namespace }}headline {{ mix }}">
    <{{ tag }} class="{{ namespace }}headline__title">{{ title }}</{{ tag }}>
    <p class="{{ namespace }}headline__subtitle">{{ subtitle }}</p>
</div>
