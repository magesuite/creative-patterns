<ul class="{{ namespace }}items-grid-layout {{ mix }}">
    {% for item in items %}
        <li class="{{ namespace }}items-grid-layout__item {% if loop.first %}{{ namespace }}items-grid-layout__item--hero{% endif %} {{ item.mix }}">
            {{ item.content }}
        </li>
    {% endfor %}
</ul>
